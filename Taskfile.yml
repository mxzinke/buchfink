version: '3'

vars:
  APP_NAME: "buchfink"
  BIN_DIR: "bin"
  PACKAGE_MANAGER: '{{.PACKAGE_MANAGER | default "npm"}}'
  VITE_PORT: '{{.WAILS_VITE_PORT | default 9245}}'
  # Target OS for build/package/run. Defaults to the host OS, and is overridden
  # by `wails3 build GOOS=...` (or the GOOS env var) for cross-compilation. The
  # tasks below dispatch to the matching platform Taskfile via this variable.
  GOOS: '{{.GOOS | default OS}}'

includes:
  common: ./build/Taskfile.yml
  windows: ./build/windows/Taskfile.yml
  darwin: ./build/darwin/Taskfile.yml
  linux: ./build/linux/Taskfile.yml

tasks:
  test:
    summary: Runs the Go test suite
    # Not `go test ./...`: main.go embeds frontend/dist, which only exists after a
    # frontend build. Listing the packages that have tests keeps the suite
    # runnable from a plain checkout.
    #
    # internal/wailsbridge is not in this list: it pulls in Wails v3 with its
    # GTK and WebKit headers, and `task test` must stay runnable on a machine
    # that has none. Those tests live in `test:bridge` and run wherever the app
    # itself can be built.
    cmds:
      - go test ./internal/accounting/... ./internal/bank/... ./internal/domain/...
        ./internal/ebilanz/... ./internal/einvoice/... ./internal/export/... ./internal/invoice/...
        ./internal/receiptstore/... ./internal/repository/... ./internal/security/...
        ./internal/service/... ./internal/timestamp/... {{.CLI_ARGS}}

  vet:
    summary: Runs go vet over the same packages as the test suite
    cmds:
      - go vet ./internal/accounting/... ./internal/bank/... ./internal/domain/...
        ./internal/ebilanz/... ./internal/einvoice/... ./internal/export/... ./internal/invoice/...
        ./internal/receiptstore/... ./internal/repository/... ./internal/security/...
        ./internal/service/... ./internal/timestamp/...

  test:bridge:
    summary: Runs vet and the tests of the Wails bridge (needs the GTK/WebKit headers)
    # The read-only audit mode lives in the bridge, and a source-level test holds
    # every bridge method to a classification of "reads" or "writes". A test that
    # never runs holds nothing — so run this task wherever the app can be built,
    # which is anywhere `task build` works.
    cmds:
      - go vet ./internal/wailsbridge/...
      - go test ./internal/wailsbridge/... {{.CLI_ARGS}}

  check:design:
    summary: Checks the frontend against the token layer of docs/design-konzept.md
    cmds:
      - python3 scripts/check_design_tokens.py

  check:bridge:
    summary: Checks that frontend/src/services/bridge.ts matches the Go bridge
    cmds:
      - python3 scripts/check_bridge_bindings.py

  check:
    summary: Formatting, vet and tests — what to run before committing
    # `test:bridge` is deliberately not part of this: it needs the GTK/WebKit
    # headers, and `task check` has to work on a plain checkout. On a machine
    # that can build the app, run `task test:bridge` alongside it.
    cmds:
      - gofmt -l internal main.go
      - task: vet
      - task: test
      - task: check:design
      - task: check:bridge

  build:
    summary: Builds the application
    cmds:
      - task: "{{.GOOS}}:build"

  package:
    summary: Packages a production build of the application
    cmds:
      - task: "{{.GOOS}}:package"

  run:
    summary: Runs the application
    cmds:
      - task: "{{.GOOS}}:run"

  dev:
    summary: Runs the application in development mode
    cmds:
      - wails3 dev -config ./build/config.yml -port {{.VITE_PORT}}

  setup:docker:
    summary: Builds Docker image for cross-compilation (~800MB download)
    cmds:
      - task: common:setup:docker

  build:server:
    summary: Builds the application in server mode (no GUI, HTTP server only)
    cmds:
      - task: common:build:server

  run:server:
    summary: Runs the application in server mode
    cmds:
      - task: common:run:server

  build:docker:
    summary: Builds a Docker image for server mode deployment
    cmds:
      - task: common:build:docker

  run:docker:
    summary: Builds and runs the Docker image
    cmds:
      - task: common:run:docker

  notices:
    summary: Regenerates THIRD-PARTY-NOTICES.md from the actual dependencies
    cmds:
      - go mod download
      - "{{.PACKAGE_MANAGER}} --prefix frontend install"
      - python3 scripts/gen_third_party_notices.py

  pages:preview:
    summary: Serves website/ locally at http://127.0.0.1:8000
    cmds:
      - python3 -m http.server 8000 --directory website

  pages:publish:
    summary: Publishes website/ to the gh-pages branch
    # Schiebt den Inhalt von website/ als Wurzel des Zweigs gh-pages. In den
    # Repository-Einstellungen steht Pages auf „Deploy from a branch", Zweig
    # gh-pages, Ordner „/ (root)" — die Veröffentlichung läuft damit von Hand
    # und ohne CI. Nach jeder Änderung an der Seite ausführen.
    #
    # Weist der Push zurück, weil gh-pages auseinandergelaufen ist, hilft die
    # ausdrückliche Form:
    #   git push origin $(git subtree split --prefix website HEAD):gh-pages --force
    cmds:
      - git subtree push --prefix website origin gh-pages
